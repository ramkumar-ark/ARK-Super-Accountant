---
phase: 03-masters-tds-gst-mapping-extension
plan: 02
type: tdd
wave: 2
depends_on:
  - "03-01"
files_modified:
  - Service/superaccountant/src/main/java/com/arktech/superaccountant/masters/rules/TdsSectionMappingRule.java
  - Service/superaccountant/src/main/java/com/arktech/superaccountant/masters/rules/GstApplicabilityRule.java
  - Service/superaccountant/src/main/java/com/arktech/superaccountant/masters/rules/HsnSacCodeRule.java
  - Service/superaccountant/src/main/java/com/arktech/superaccountant/masters/rules/GstinPresenceRule.java
  - Service/superaccountant/src/main/java/com/arktech/superaccountant/masters/rules/MismatchDetectionRule.java
  - Service/superaccountant/src/test/java/com/arktech/superaccountant/masters/rules/TdsSectionMappingRuleTest.java
  - Service/superaccountant/src/test/java/com/arktech/superaccountant/masters/rules/GstApplicabilityRuleTest.java
  - Service/superaccountant/src/test/java/com/arktech/superaccountant/masters/rules/HsnSacCodeRuleTest.java
  - Service/superaccountant/src/test/java/com/arktech/superaccountant/masters/rules/GstinPresenceRuleTest.java
autonomous: true
requirements:
  - MSTR-02
  - MSTR-03
  - MSTR-04
  - MSTR-05

must_haves:
  truths:
    - "TdsSectionMappingRule emits MEDIUM finding for PURCHASE/EXPENSE/INCOME ledger with null tdsSection; HIGH for TDS category; LOW for GST/OTHER"
    - "TdsSectionMappingRule emits no finding when tdsSection is set to any non-null value"
    - "GstApplicabilityRule emits MEDIUM finding for INCOME/GST ledger with null gstApplicabilityType; LOW for other categories"
    - "HsnSacCodeRule emits MEDIUM finding for INCOME/GST ledger with null hsnSacCode; no finding for PURCHASE/EXPENSE ledgers"
    - "GstinPresenceRule emits HIGH finding for PURCHASE ledger with null gstin; no finding for non-PURCHASE ledgers"
    - "MismatchDetectionRule updated to use HIGH/MEDIUM/LOW severity values (not INFO/WARNING/ERROR)"
    - "All 4 rule classes are @Component beans — ValidationOrchestrator auto-discovers them"
    - "Unit tests for all 4 new rules pass with RED-first discipline"
  artifacts:
    - path: "Service/superaccountant/src/main/java/com/arktech/superaccountant/masters/rules/TdsSectionMappingRule.java"
      provides: "TDS section completeness check"
      exports: ["TdsSectionMappingRule"]
    - path: "Service/superaccountant/src/main/java/com/arktech/superaccountant/masters/rules/GstApplicabilityRule.java"
      provides: "GST applicability completeness check"
    - path: "Service/superaccountant/src/main/java/com/arktech/superaccountant/masters/rules/HsnSacCodeRule.java"
      provides: "HSN/SAC code completeness check"
    - path: "Service/superaccountant/src/main/java/com/arktech/superaccountant/masters/rules/GstinPresenceRule.java"
      provides: "GSTIN presence check for PURCHASE ledgers"
    - path: "Service/superaccountant/src/test/java/com/arktech/superaccountant/masters/rules/TdsSectionMappingRuleTest.java"
      provides: "Unit tests for TDS section rule — MSTR-02, MSTR-03"
    - path: "Service/superaccountant/src/test/java/com/arktech/superaccountant/masters/rules/GstinPresenceRuleTest.java"
      provides: "Unit tests for GSTIN rule — MSTR-05"
  key_links:
    - from: "TdsSectionMappingRule"
      to: "context.preconfiguredMasters()"
      via: "ValidationContext.preconfiguredMasters() iteration"
      pattern: "context.preconfiguredMasters()"
    - from: "@Component(\"TDS_SECTION_MAPPING\")"
      to: "ValidationOrchestrator.ruleMap"
      via: "Spring bean name lookup"
      pattern: "@Component\\(\"TDS_SECTION_MAPPING\"\\)"
---

<objective>
Implement the 4 new ValidationRule classes with full TDD discipline, and update MismatchDetectionRule to use HIGH/MEDIUM/LOW severity values.

Purpose: These rules are the core business logic of Phase 3. They check master configuration completeness (not upload-vs-config comparison) because Tally JSON does not export TDS section, GST applicability type, HSN/SAC, or GSTIN per ledger. Missing metadata in PreconfiguredMaster rows is surfaced as findings that OPERATORS can resolve.

Output: 4 new @Component rule classes + updated MismatchDetectionRule + 4 unit test classes (RED-GREEN-REFACTOR committed separately).
</objective>

<execution_context>
@F:\Software Projects\Super Accountant\.planning\phases\03-masters-tds-gst-mapping-extension\execute-plan.md
@F:\Software Projects\Super Accountant\.planning\phases\03-masters-tds-gst-mapping-extension\summary.md
</execution_context>

<context>
@F:\Software Projects\Super Accountant\.planning\PROJECT.md
@F:\Software Projects\Super Accountant\.planning\ROADMAP.md
@F:\Software Projects\Super Accountant\.planning\phases\03-masters-tds-gst-mapping-extension\03-RESEARCH.md
@F:\Software Projects\Super Accountant\.planning\phases\03-masters-tds-gst-mapping-extension\03-PATTERNS.md

<interfaces>
<!-- Key contracts from Plan 01 output and existing analogs -->

From masters/rules/MismatchDetectionRule.java (exact implementation pattern):
```java
@Component("MISMATCH_DETECTION")
public class MismatchDetectionRule implements ValidationRule {

    @Override
    public String getRuleCode() { return "MISMATCH_DETECTION"; }

    @Override
    public List<ValidationFinding> execute(ValidationContext context, List<ParsedLedger> parsedLedgers) {
        List<ValidationFinding> findings = new ArrayList<>();
        // business logic iterates parsedLedgers + context.preconfiguredMasters()
        return findings;
    }
}
```

From masters/models/ValidationContext.java (confirmed record):
```java
public record ValidationContext(
    UUID organizationId,
    String uploadedBy,
    List<PreconfiguredMaster> preconfiguredMasters,
    Map<String, String> settings
) {}
```

From test/masters/rules/MismatchDetectionRuleTest.java (exact test pattern):
```java
class MismatchDetectionRuleTest {
    private MismatchDetectionRule rule;

    @BeforeEach
    void setUp() { rule = new MismatchDetectionRule(); }

    private ValidationContext ctx(List<PreconfiguredMaster> masters) {
        return new ValidationContext(UUID.randomUUID(), "testuser", masters, Map.of());
    }
}
```

Rule severity logic (from RESEARCH.md):
- TdsSectionMappingRule: TDS category → HIGH; PURCHASE/EXPENSE/INCOME → MEDIUM; default → LOW
- GstApplicabilityRule: INCOME/GST category → MEDIUM; default → LOW
- HsnSacCodeRule: INCOME/GST category → MEDIUM (regardless of gstApplicabilityType in Phase 3)
- GstinPresenceRule: PURCHASE category → HIGH only

Phase 3 rules MUST iterate context.preconfiguredMasters() NOT parsedLedgers.
Pass empty List.of() for parsedLedgers parameter in tests — Tally does not export these fields.

ValidationFinding fields to set in each finding:
- ruleCode: getRuleCode()
- ledgerName: master.getLedgerName()
- category: master.getCategory()
- severity: calculated severity
- resolveStatus: ResolveStatus.OPEN
- message: human-readable string naming the ledger
- suggestedFix: actionable string
- mismatchType: leave null (no MismatchType applies to completeness checks)

MismatchDetectionRule current severity values to replace:
- FindingSeverity.ERROR → FindingSeverity.HIGH
- FindingSeverity.WARNING → FindingSeverity.MEDIUM
- FindingSeverity.INFO → FindingSeverity.LOW
</interfaces>
</context>

<feature>
  <name>Four new ValidationRule implementations with unit test coverage</name>
  <files>
    TdsSectionMappingRule.java, GstApplicabilityRule.java, HsnSacCodeRule.java, GstinPresenceRule.java,
    MismatchDetectionRule.java (severity update),
    TdsSectionMappingRuleTest.java, GstApplicabilityRuleTest.java, HsnSacCodeRuleTest.java, GstinPresenceRuleTest.java
  </files>
  <behavior>
    TdsSectionMappingRule (ruleCode = "TDS_SECTION_MAPPING"):
    - input: PreconfiguredMaster with category=TDS, tdsSection=null → finding with severity=HIGH
    - input: PreconfiguredMaster with category=PURCHASE, tdsSection=null → finding with severity=MEDIUM
    - input: PreconfiguredMaster with category=EXPENSE, tdsSection=null → finding with severity=MEDIUM
    - input: PreconfiguredMaster with category=INCOME, tdsSection=null → finding with severity=MEDIUM
    - input: PreconfiguredMaster with category=GST, tdsSection=null → finding with severity=LOW
    - input: PreconfiguredMaster with category=OTHER, tdsSection=null → finding with severity=LOW
    - input: PreconfiguredMaster with tdsSection="194C" (any non-null) → no finding
    - input: PreconfiguredMaster with tdsSection="NOT_SUBJECT" → no finding
    - empty context.preconfiguredMasters() → no findings (empty list)

    GstApplicabilityRule (ruleCode = "GST_APPLICABILITY"):
    - input: PreconfiguredMaster with category=INCOME, gstApplicabilityType=null → finding MEDIUM
    - input: PreconfiguredMaster with category=GST, gstApplicabilityType=null → finding MEDIUM
    - input: PreconfiguredMaster with category=PURCHASE, gstApplicabilityType=null → finding LOW
    - input: PreconfiguredMaster with gstApplicabilityType=TAXABLE (any non-null) → no finding

    HsnSacCodeRule (ruleCode = "HSN_SAC_CODE"):
    - input: PreconfiguredMaster with category=INCOME, hsnSacCode=null → finding MEDIUM
    - input: PreconfiguredMaster with category=GST, hsnSacCode=null → finding MEDIUM
    - input: PreconfiguredMaster with category=PURCHASE, hsnSacCode=null → no finding (purchase doesn't require HSN)
    - input: PreconfiguredMaster with category=INCOME, hsnSacCode="998311" → no finding

    GstinPresenceRule (ruleCode = "GSTIN_PRESENCE"):
    - input: PreconfiguredMaster with category=PURCHASE, gstin=null → finding HIGH
    - input: PreconfiguredMaster with category=EXPENSE, gstin=null → no finding
    - input: PreconfiguredMaster with category=INCOME, gstin=null → no finding
    - input: PreconfiguredMaster with category=PURCHASE, gstin="29ABCDE1234F1Z5" → no finding

    MismatchDetectionRule (update only):
    - All FindingSeverity.ERROR references → FindingSeverity.HIGH
    - All FindingSeverity.WARNING references → FindingSeverity.MEDIUM
    - All FindingSeverity.INFO references → FindingSeverity.LOW
    - All existing MismatchDetectionRuleTest cases must still pass
  </behavior>
  <implementation>
    RED cycle: Write all 4 test classes first. Each test class follows MismatchDetectionRuleTest pattern exactly:
    - Same package: com.arktech.superaccountant.masters.rules
    - @BeforeEach sets up rule instance (no Spring context needed — pure unit tests)
    - Helper factory method `configured(name, category, fieldValue)` builds PreconfiguredMaster test instances
    - Helper `ctx(List<PreconfiguredMaster>)` builds ValidationContext
    - Tests call rule.execute(ctx, List.of()) — empty parsedLedgers
    - Assert findings.size() and findings.get(0).getSeverity()

    GREEN cycle: Implement each rule class:
    - Package: com.arktech.superaccountant.masters.rules
    - @Component("RULE_CODE") annotation — bean name must match ruleCode string (ValidationOrchestrator lookup)
    - implements ValidationRule
    - iterate context.preconfiguredMasters() only
    - switch expression for severity logic
    - set ruleCode, ledgerName, category, severity, resolveStatus=OPEN, message, suggestedFix on each finding

    REFACTOR: Update MismatchDetectionRule to use HIGH/MEDIUM/LOW.

    TDD commit sequence:
    1. `test(03-02): add failing unit tests for 4 new validation rules`
    2. `feat(03-02): implement TdsSectionMappingRule, GstApplicabilityRule, HsnSacCodeRule, GstinPresenceRule`
    3. `refactor(03-02): update MismatchDetectionRule severity to HIGH/MEDIUM/LOW`
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ValidationOrchestrator → ValidationRule.execute() | Rule receives context from orchestrator; no user input directly |
| Rule output → ValidationFinding persistence | Finding fields populated by rule; values are string literals, not user-supplied data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | ValidationRule.execute output | accept | Rule output is server-generated string literals (ledger names from DB, severity enum values); no user input reaches finding construction |
| T-03-08 | Denial of Service | TdsSectionMappingRule iterating large master list | accept | List iteration is O(n) where n = org master count (typically <200 ledgers); no external I/O in rule execution; acceptable performance |
| T-03-09 | Information Disclosure | Finding message contains ledger name | accept | Ledger names are org-internal data; findings are only accessible to authenticated org members with OPERATOR/ACCOUNTANT/OWNER/AUDITOR_CA roles via existing @PreAuthorize guards on UploadController |
</threat_model>

<verification>
After plan execution:

1. Unit tests: `cd Service/superaccountant && ./mvnw test -Dtest="TdsSectionMappingRuleTest,GstApplicabilityRuleTest,HsnSacCodeRuleTest,GstinPresenceRuleTest" -pl .` — all must pass
2. Full test suite: `cd Service/superaccountant && ./mvnw test -pl .` — must pass including MismatchDetectionRuleTest
3. Component annotation check: grep -r "@Component" Service/superaccountant/src/main/java/com/arktech/superaccountant/masters/rules/ — must show all 5 rules including the 4 new ones
4. Severity check: grep "FindingSeverity.ERROR\|FindingSeverity.WARNING\|FindingSeverity.INFO" Service/superaccountant/src/main/java/com/arktech/superaccountant/masters/rules/MismatchDetectionRule.java — must return no matches (all replaced with HIGH/MEDIUM/LOW)
</verification>

<success_criteria>
- 4 new rule classes exist with correct @Component annotation names
- All 4 new rule unit test classes pass (GREEN)
- MismatchDetectionRuleTest still passes after severity rename
- No FindingSeverity.ERROR/WARNING/INFO references remain in MismatchDetectionRule
- ./mvnw test -pl Service/superaccountant passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-masters-tds-gst-mapping-extension/03-02-PLAN-SUMMARY.md` following the summary template.
</output>
