---
phase: 2
plan: 2
wave: 2
depends_on: [1]
files_modified:
  - Service/superaccountant/src/main/java/com/arktech/superaccountant/masters/controllers/UploadController.java
  - Service/superaccountant/src/main/java/com/arktech/superaccountant/masters/controllers/PreconfiguredMastersController.java
  - Service/superaccountant/src/main/java/com/arktech/superaccountant/gst/GstValidationController.java
  - Service/superaccountant/src/main/java/com/arktech/superaccountant/tally/TallyImportController.java
  - Service/superaccountant/src/main/java/com/arktech/superaccountant/masters/controllers/OrganizationController.java
  - .planning/phases/02-role-restructuring-ca-multi-client-switching/PERMISSION_MATRIX.md
  - Service/superaccountant/src/test/java/com/arktech/superaccountant/masters/controllers/UploadControllerIT.java
  - Service/superaccountant/src/test/java/com/arktech/superaccountant/masters/controllers/OrganizationControllerIT.java
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
tags: [auth, rbac, permissions, api]
must_haves:
  truths:
    - "OWNER can GET all read-only endpoints but POST /api/v1/uploads returns 403"
    - "ACCOUNTANT can POST /api/v1/uploads and PATCH /api/v1/uploads/{id}/mismatches/{fid}/resolve (200)"
    - "OPERATOR can POST /api/v1/uploads and resolve mismatches (200) but cannot POST /api/organizations (403)"
    - "AUDITOR_CA can GET /api/v1/uploads (200) but POST /api/v1/uploads returns 403"
    - "AUDITOR_CA can POST /api/organizations/{id}/select and receive a new JWT carrying the new organizationId"
    - "No @PreAuthorize expression in the backend references DATA_ENTRY_OPERATOR"
  artifacts:
    - path: ".planning/phases/02-role-restructuring-ca-multi-client-switching/PERMISSION_MATRIX.md"
      provides: "Documented permission matrix (source of truth for guard mappings)"
      contains: "AUDITOR_CA"
    - path: "Service/superaccountant/src/test/java/com/arktech/superaccountant/masters/controllers/UploadControllerIT.java"
      provides: "Integration coverage of role×endpoint matrix"
      contains: "AUDITOR_CA"
  key_links:
    - from: "UploadController"
      to: "Spring Security @PreAuthorize"
      via: "hasRole('ACCOUNTANT') or hasRole('OPERATOR')"
      pattern: "hasRole\\('OPERATOR'\\)"
    - from: "OrganizationController.selectOrganization"
      to: "JwtUtils.generateJwtTokenForUser"
      via: "new JWT with organizationId claim, no re-auth"
      pattern: "generateJwtTokenForUser"
---

# Plan 02: Permission Matrix & @PreAuthorize Guard Restructuring

## Goal

Apply the documented permission matrix to every existing `@PreAuthorize` annotation: remove `DATA_ENTRY_OPERATOR`, add `OPERATOR` for data-entry actions, add `AUDITOR_CA` to read-only endpoints, and reduce OWNER to read-only on operational content. Verify with integration tests covering all four roles × all guarded endpoints.

## Tasks

<task id="2.1" title="Wave 0: Write failing integration tests for role × endpoint matrix">
<read_first>
- Service/superaccountant/src/main/java/com/arktech/superaccountant/masters/controllers/UploadController.java — list of guarded endpoints and current guards (DATA_ENTRY_OPERATOR present)
- Service/superaccountant/src/main/java/com/arktech/superaccountant/masters/controllers/OrganizationController.java — select endpoint returns 200 with token/organizationId/organizationName/role
- Service/superaccountant/src/test/java/com/arktech/superaccountant/repository/UserRepositoryIT.java — Testcontainers boilerplate
- Service/superaccountant/src/main/java/com/arktech/superaccountant/login/security/jwt/JwtUtils.java — `generateJwtTokenForUser(String username, UUID organizationId)` signature for minting test JWTs (note: organizationId is java.util.UUID, not String)
- .planning/phases/02-role-restructuring-ca-multi-client-switching/02-RESEARCH.md — Pattern 4 permission matrix
</read_first>
<action>
Create two Testcontainers integration tests under `Service/superaccountant/src/test/java/com/arktech/superaccountant/masters/controllers/`. Both use `@SpringBootTest`, `@AutoConfigureMockMvc`, `@Testcontainers`, `@Transactional`, and the same PostgreSQLContainer + `@DynamicPropertySource` + `@TestPropertySource` pattern as task 1.1 (JWT_SECRET via @TestPropertySource, datasource via @DynamicPropertySource).

Helper approach: each test sets up four users in a `@BeforeEach` (via `UserRepository` and `RoleRepository` direct saves) — `ownerUser`, `accountantUser`, `operatorUser`, `auditorUser` — and mints JWTs via `@Autowired JwtUtils jwtUtils` calling `jwtUtils.generateJwtTokenForUser(username, orgId)` where `orgId` is a `java.util.UUID` (pass `null` or a saved org UUID). Use `MockMvc.perform(...).header("Authorization", "Bearer " + jwt)`.

### File 1: `UploadControllerIT.java`

Scenarios (each one assertion, status code only — body shape not relevant here):

| # | Role | Method + Path | Expected Status |
|---|------|---------------|-----------------|
| 1 | OWNER | POST /api/v1/uploads (multipart fake file) | 403 |
| 2 | ACCOUNTANT | POST /api/v1/uploads | NOT 403 (200 or 400 depending on file validation; assert `status().value() != 403`) |
| 3 | OPERATOR | POST /api/v1/uploads | NOT 403 |
| 4 | AUDITOR_CA | POST /api/v1/uploads | 403 |
| 5 | OWNER | GET /api/v1/uploads | NOT 403 |
| 6 | AUDITOR_CA | GET /api/v1/uploads | NOT 403 |
| 7 | OWNER | PATCH /api/v1/uploads/00000000-0000-0000-0000-000000000000/mismatches/00000000-0000-0000-0000-000000000000/resolve with empty body | 403 |
| 8 | ACCOUNTANT | same PATCH | NOT 403 (will be 404 because job doesn't exist; that's fine) |
| 9 | OPERATOR | same PATCH | NOT 403 |
| 10 | AUDITOR_CA | same PATCH | 403 |
| 11 | no-JWT request | POST /api/v1/uploads | 401 |

### File 2: `OrganizationControllerIT.java`

Scenarios:

| # | Role | Method + Path | Expected |
|---|------|---------------|----------|
| 1 | OPERATOR | POST /api/organizations body `{"name":"New Org"}` | 403 |
| 2 | AUDITOR_CA | POST /api/organizations body `{"name":"New Org"}` | 403 |
| 3 | OWNER | POST /api/organizations body `{"name":"New Org"}` | 201 |
| 4 | ACCOUNTANT | POST /api/organizations body `{"name":"New Org"}` | 201 |
| 5 | AUDITOR_CA | POST /api/organizations/{orgId}/select (where user has UserOrganization membership with role AUDITOR_CA) | 200 AND JSON response contains non-null `token` AND `organizationId` equals requested orgId AND `role` equals `"ROLE_AUDITOR_CA"` |
| 6 | AUDITOR_CA | POST /api/organizations/{other-org-id}/select where user is NOT a member of that org | 403 with message containing "Could not switch" |
| 7 | After scenario 5, parse the returned token with `jwtUtils.getOrganizationIdFromJwtToken(token)` → assert equals the requested orgId (proves AUTH-06: switch does not require re-auth and JWT carries new org) |

For scenario 5 setup: persist an `Organization`, a `User` with ERole.ROLE_AUDITOR_CA, and a `UserOrganization` row linking them with role AUDITOR_CA — using the in-test JPA repositories. The authenticating JWT is minted for that user with a DIFFERENT active orgId (to prove the switch changes it).

All tests MUST fail now because the current guards still contain DATA_ENTRY_OPERATOR (not OPERATOR) — scenarios 3, 8, 9 for `OPERATOR` role will return 403 on current code. Commit in RED state.
</action>
<acceptance_criteria>
- [ ] File `UploadControllerIT.java` exists in `.../masters/controllers/`
- [ ] File `OrganizationControllerIT.java` exists in `.../masters/controllers/`
- [ ] grep: `UploadControllerIT.java` contains `ROLE_AUDITOR_CA`, `ROLE_OPERATOR`, `ROLE_OWNER`, `ROLE_ACCOUNTANT`
- [ ] grep: `UploadControllerIT.java` contains `/api/v1/uploads` AND `.isForbidden()` (at least once)
- [ ] grep: `OrganizationControllerIT.java` contains `/select` AND `getOrganizationIdFromJwtToken`
- [ ] `./mvnw test -pl Service/superaccountant -Dtest=UploadControllerIT` → FAILS (OPERATOR scenarios fail because guard still lists DATA_ENTRY_OPERATOR)
</acceptance_criteria>
</task>

<task id="2.2" title="Document permission matrix for the repo (source of truth)">
<read_first>
- .planning/phases/02-role-restructuring-ca-multi-client-switching/02-RESEARCH.md — Pattern 4 and derived guard mappings
- ROADMAP.md — Phase 2 success criteria
</read_first>
<action>
Create `.planning/phases/02-role-restructuring-ca-multi-client-switching/PERMISSION_MATRIX.md` with exactly this structure:

```markdown
# Phase 2 Permission Matrix

Source of truth for @PreAuthorize expressions in Super Accountant.
Every backend guard must reference this table. Do not diverge without updating this document in the same commit.

## Roles

- ROLE_OWNER — administrative; cannot do data entry or resolve mismatches
- ROLE_ACCOUNTANT — operational; can do everything including approve AI entries (future)
- ROLE_OPERATOR — operational; reconciliation + resolve, cannot approve AI entries
- ROLE_AUDITOR_CA — read-only cross-org access (CA use case)

## Endpoint Matrix

| Endpoint | Method | OWNER | ACCOUNTANT | OPERATOR | AUDITOR_CA | Guard Expression |
|----------|--------|-------|------------|----------|------------|------------------|
| /api/organizations | POST | YES | YES | NO | NO | `hasRole('OWNER') or hasRole('ACCOUNTANT')` |
| /api/organizations/{id} | GET | YES | YES | YES | YES | `isAuthenticated()` |
| /api/organizations/{id}/invites | POST | YES | YES | NO | NO | `hasRole('OWNER') or hasRole('ACCOUNTANT')` |
| /api/organizations/me/list | GET | YES | YES | YES | YES | `isAuthenticated()` |
| /api/organizations/{id}/select | POST | YES | YES | YES | YES | `isAuthenticated()` |
| /api/v1/uploads | POST | NO | YES | YES | NO | `hasRole('ACCOUNTANT') or hasRole('OPERATOR')` |
| /api/v1/uploads | GET | YES | YES | YES | YES | `isAuthenticated()` |
| /api/v1/uploads/{id}/mismatches | GET | YES | YES | YES | YES | `isAuthenticated()` |
| /api/v1/uploads/{id}/mismatches/export | GET | YES | YES | YES | YES | `isAuthenticated()` |
| /api/v1/uploads/{jobId}/mismatches/{findingId}/resolve | PATCH | NO | YES | YES | NO | `hasRole('ACCOUNTANT') or hasRole('OPERATOR')` |
| /api/v1/validation-rules | GET | YES | YES | YES | YES | `isAuthenticated()` |
| /api/v1/preconfigured-masters | GET | YES | YES | YES | YES | `isAuthenticated()` |
| /api/v1/preconfigured-masters | POST | NO | YES | YES | NO | `hasRole('ACCOUNTANT') or hasRole('OPERATOR')` |
| /api/v1/preconfigured-masters/{id} | PUT | NO | YES | YES | NO | `hasRole('ACCOUNTANT') or hasRole('OPERATOR')` |
| /api/v1/preconfigured-masters/{id} | DELETE | YES | YES | NO | NO | `hasRole('OWNER') or hasRole('ACCOUNTANT')` |
| /api/v1/preconfigured-masters/bulk | POST | NO | YES | YES | NO | `hasRole('ACCOUNTANT') or hasRole('OPERATOR')` |
| /api/v1/preconfigured-masters/onboard | POST | YES | YES | NO | NO | `hasRole('OWNER') or hasRole('ACCOUNTANT')` |
| /api/gst/validate | POST | NO | YES | YES | NO | `hasRole('ACCOUNTANT') or hasRole('OPERATOR')` |
| /api/tally/import | POST | NO | YES | YES | NO | `hasRole('ACCOUNTANT') or hasRole('OPERATOR')` |

## Derivation Rules

- Read (GET, list, export): `isAuthenticated()` — all four roles can read once authenticated. Guarding this tighter (enumerating all 4 roles) is equivalent to `isAuthenticated()` since every authenticated user has exactly one of the 4 roles.
- Data entry (upload, create master, bulk, validate, import): `hasRole('ACCOUNTANT') or hasRole('OPERATOR')`
- Resolve mismatches: `hasRole('ACCOUNTANT') or hasRole('OPERATOR')`
- Admin (invite, create org, delete master, onboard): `hasRole('OWNER') or hasRole('ACCOUNTANT')`
- AI approve endpoints (future, not in Phase 2): `hasRole('ACCOUNTANT')` only

## Permission Reduction from Phase 1

OWNER previously allowed on `POST /api/v1/uploads`, resolve, bulk, create/update master, `/api/gst/validate`, `/api/tally/import`. Under the new matrix, OWNER is removed from all data-entry guards. Existing seed/demo OWNER accounts must delegate data entry to an ACCOUNTANT or OPERATOR.
```

This file is the spec that Task 2.3's guard edits must match exactly.
</action>
<acceptance_criteria>
- [ ] File `.planning/phases/02-role-restructuring-ca-multi-client-switching/PERMISSION_MATRIX.md` exists
- [ ] grep: file contains `hasRole('ACCOUNTANT') or hasRole('OPERATOR')`
- [ ] grep: file contains `AUDITOR_CA`
- [ ] grep: file contains `/api/v1/uploads` and `PATCH`
</acceptance_criteria>
</task>

<task id="2.3" title="Rewrite every @PreAuthorize expression to match the matrix">
<read_first>
- .planning/phases/02-role-restructuring-ca-multi-client-switching/PERMISSION_MATRIX.md — target expressions
- Service/superaccountant/src/main/java/com/arktech/superaccountant/masters/controllers/UploadController.java — 5 annotated methods
- Service/superaccountant/src/main/java/com/arktech/superaccountant/masters/controllers/PreconfiguredMastersController.java — 6 annotated methods
- Service/superaccountant/src/main/java/com/arktech/superaccountant/gst/controllers/GstValidationController.java — 1 guarded endpoint
- Service/superaccountant/src/main/java/com/arktech/superaccountant/tally/controllers/TallyImportController.java — 1 guarded endpoint
- Service/superaccountant/src/main/java/com/arktech/superaccountant/masters/controllers/OrganizationController.java — 5 annotated methods
</read_first>
<action>
Apply these exact string replacements in each file. Use the PERMISSION_MATRIX.md table as the definitive target. Every `hasRole('DATA_ENTRY_OPERATOR')` must be replaced with `hasRole('OPERATOR')` at minimum; additionally, OWNER is removed from data-entry guards.

### UploadController.java
- `POST /api/v1/uploads` (method annotated with `@PostMapping` for uploads): replace
  `@PreAuthorize("hasRole('OWNER') or hasRole('ACCOUNTANT') or hasRole('DATA_ENTRY_OPERATOR')")`
  with
  `@PreAuthorize("hasRole('ACCOUNTANT') or hasRole('OPERATOR')")`
- `PATCH /api/v1/uploads/{jobId}/mismatches/{findingId}/resolve`: replace same old expression with
  `@PreAuthorize("hasRole('ACCOUNTANT') or hasRole('OPERATOR')")`
- GET endpoints (`GET /api/v1/uploads`, `/mismatches`, `/mismatches/export`, `/api/v1/validation-rules`): leave `@PreAuthorize("isAuthenticated()")` unchanged.

### PreconfiguredMastersController.java
- `POST /api/v1/preconfigured-masters`, `PUT /api/v1/preconfigured-masters/{id}`, `POST /api/v1/preconfigured-masters/bulk`: change
  `@PreAuthorize("hasRole('OWNER') or hasRole('ACCOUNTANT') or hasRole('DATA_ENTRY_OPERATOR')")`
  to
  `@PreAuthorize("hasRole('ACCOUNTANT') or hasRole('OPERATOR')")`
- `DELETE /api/v1/preconfigured-masters/{id}`, `POST /api/v1/preconfigured-masters/onboard`: leave
  `@PreAuthorize("hasRole('OWNER') or hasRole('ACCOUNTANT')")` unchanged.
- `GET /api/v1/preconfigured-masters`: leave `isAuthenticated()` unchanged.

### GstValidationController.java
- `POST /api/gst/validate`: change
  `@PreAuthorize("hasRole('OWNER') or hasRole('ACCOUNTANT') or hasRole('DATA_ENTRY_OPERATOR')")`
  to
  `@PreAuthorize("hasRole('ACCOUNTANT') or hasRole('OPERATOR')")`

### TallyImportController.java
- `POST /api/tally/import`: change
  `@PreAuthorize("hasRole('OWNER') or hasRole('ACCOUNTANT') or hasRole('DATA_ENTRY_OPERATOR')")`
  to
  `@PreAuthorize("hasRole('ACCOUNTANT') or hasRole('OPERATOR')")`

### OrganizationController.java
- All 5 annotations stay as they are (`hasRole('OWNER') or hasRole('ACCOUNTANT')` for create/invite; `isAuthenticated()` for get/list/select). These already match the matrix.

After edits, repo-wide grep for `DATA_ENTRY_OPERATOR` in `*.java` must return zero matches. Also grep-verify no guard expression accidentally contains `hasRole('ROLE_...')` (Spring Security auto-prepends ROLE_; passing `ROLE_OPERATOR` to `hasRole` would require `ROLE_ROLE_OPERATOR` — a subtle bug).
</action>
<acceptance_criteria>
- [ ] grep: `grep -r "DATA_ENTRY_OPERATOR" Service/superaccountant/src/main/java` returns 0 matches
- [ ] grep: `UploadController.java` contains `@PreAuthorize("hasRole('ACCOUNTANT') or hasRole('OPERATOR')")` (at least twice — POST and PATCH)
- [ ] grep: `PreconfiguredMastersController.java` contains `@PreAuthorize("hasRole('ACCOUNTANT') or hasRole('OPERATOR')")` (at least three times)
- [ ] grep: `GstValidationController.java` contains `hasRole('OPERATOR')`
- [ ] grep: `TallyImportController.java` contains `hasRole('OPERATOR')`
- [ ] grep: no file in `Service/superaccountant/src/main/java/.../controllers/` contains `hasRole('ROLE_` (bad double-prefix)
- [ ] `./mvnw test -pl Service/superaccountant -Dtest=UploadControllerIT,OrganizationControllerIT` exits 0 (all 18 scenarios green)
- [ ] `./mvnw test -pl Service/superaccountant` full suite exits 0 (no regressions in Plan 01 tests)
</acceptance_criteria>
</task>

## Verification

```bash
./mvnw test -pl Service/superaccountant -Dtest=UploadControllerIT,OrganizationControllerIT,AuthControllerIT,ERoleTest,DataInitializerIT
./mvnw test -pl Service/superaccountant   # full suite, no regressions
grep -r "DATA_ENTRY_OPERATOR" Service/superaccountant/src/main/java   # must be empty
```

## must_haves
- [ ] No `DATA_ENTRY_OPERATOR` substring remains in backend Java sources
- [ ] Every `POST`/`PATCH` write endpoint guarded by `hasRole('ACCOUNTANT') or hasRole('OPERATOR')`
- [ ] Admin endpoints guarded by `hasRole('OWNER') or hasRole('ACCOUNTANT')` (unchanged from Phase 1)
- [ ] AUDITOR_CA receives 403 on all write endpoints and 200 on read endpoints
- [ ] AUDITOR_CA receives a new JWT with the target `organizationId` claim from `POST /api/organizations/{id}/select`
- [ ] `PERMISSION_MATRIX.md` committed as the spec any future guard change must reference

<threat_model>
## Threats

| Threat | STRIDE | Severity | Mitigation |
|--------|--------|----------|------------|
| T-02-06: AUDITOR_CA writes data (forged/approves entries) — violates AUTH-04 | Elevation of Privilege | High | `hasRole('ACCOUNTANT') or hasRole('OPERATOR')` on all write endpoints excludes AUDITOR_CA; `UploadControllerIT` scenarios 4, 10 assert 403 |
| T-02-07: OWNER performs data entry — violates AUTH-02 (per criterion 2 of Phase 2) | Elevation of Privilege (policy violation) | Medium | OWNER removed from data-entry guards across 5 files; `UploadControllerIT` scenarios 1, 7 assert 403 for OWNER |
| T-02-08: Double-prefix typo `hasRole('ROLE_OPERATOR')` silently makes guard unmatchable → method becomes fully blocked OR fully open depending on config | Tampering/misconfig | Low | Acceptance criterion greps for `hasRole('ROLE_` across controllers; matrix doc uses plain `OPERATOR` strings |
| T-02-09: Role enumeration via 401 vs 403 differential response | Information Disclosure | Low (accept) | Spring Security returns 401 for missing JWT and 403 for insufficient authority; this differential is industry-standard and required for client error handling. No rate-limiting in Phase 2. |
| T-02-10: Cross-org data access — AUDITOR_CA selects an org they are not a member of | Information Disclosure | High | `OrganizationController.selectOrganization` already checks `userOrganizationRepository.findByUserIdAndOrganizationId` and returns 403 if empty; `OrganizationControllerIT` scenario 6 asserts this |
| T-02-11: Stale-role attack — attacker obtains JWT as OPERATOR, gets role demoted in DB, continues to use old JWT | Elevation of Privilege | Low | Research Pattern 3 confirms role is resolved from DB per request via `UserDetailsServiceImpl.loadUserByUsername`, not read from JWT. Next request after DB change uses new role. |

## ASVS L1 Notes

- **V4.1.1 Access control enforced at trusted service layer** — `@PreAuthorize` annotations evaluated by Spring Security method interceptor (server-side, after auth), not client-controlled.
- **V4.1.3 Principle of Least Privilege** — OWNER read-only on operational endpoints; AUDITOR_CA read-only everywhere; OPERATOR cannot perform admin actions.
- **V4.1.5 Deny by default** — `.anyRequest().authenticated()` in `WebSecurityConfig` plus explicit `@PreAuthorize` on every controller method (none rely on missing annotation).
- **V4.2.1 Access Control Design** — Matrix documented in `PERMISSION_MATRIX.md`; any future endpoint must reference this file.
</threat_model>
